{{> nav-block}}
<div class="container">
    <div class="row">
        <div class="col-6">
            <h2>Expired Items</h2>
            {{#each expiredItems}}
                {{> expired-block}}
            {{/each}}
        </div>
    </div>
</div>