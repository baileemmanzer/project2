{{> nav-block}}
<div class="container">
    <div class="row">
        <div class="col-6">
            {{#each expiredItems}}
                {{> expired-block}}
            {{/each}}
        </div>
    </div>
</div>