{{> nav-block}}
<div class="container">
    <div class="row">
        <div class="col-6">
            {{#each expiringItems}}
                {{> expiring-block}}
            {{/each}}
        </div>
    </div>
</div>